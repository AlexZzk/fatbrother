<view class="cart-bar">
  <view class="cart-left" bindtap="onCartTap">
    <view class="cart-icon-wrap">
      <text class="cart-icon">🛒</text>
      <view wx:if="{{count > 0}}" class="cart-badge">{{count > 99 ? '99+' : count}}</view>
    </view>
    <view class="cart-info">
      <text wx:if="{{count > 0}}" class="cart-count">已选{{count}}件</text>
      <text wx:else class="cart-empty">购物车是空的</text>
      <text wx:if="{{count > 0}}" class="cart-total">¥{{totalPrice / 100}}</text>
    </view>
  </view>
  <view class="cart-checkout {{count > 0 ? '' : 'cart-checkout--disabled'}}" bindtap="onCheckout">
    去结算
  </view>
</view>
